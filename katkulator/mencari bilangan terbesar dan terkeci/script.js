const terbesar = (b1, b2) => {
    if (b1 > b2) {
        alert(b1);
        alert(b2);
    } else if (b2 > b1) {
        alert(b2);
        alert(b1);
    } else {
        alert('nilai tidak valid');
    }
}